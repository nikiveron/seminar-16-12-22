// 3.cpp
#include <iostream>
using namespace std;

int main()
{
	setlocale(LC_ALL, "ru");
	int k, a;
	cout << "Введите кол-во эл-ов:" << endl;
	cin >> k;
	int* m = new int[k];
	cout << "Введите эл-ты массива:" << endl;
	for (int i = 0; i<k;i++){
		cin >> a;
		m[i] = a;
	}
	cout << "Индексация начинается с нуля!" << endl;
	for (int i = 0; i < k; i++) {
		if (m[i] == 0) cout << i << " ";
	}
}
